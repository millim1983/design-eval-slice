# docker-compose.local.yml
services:
  api:
    build: ./backend
    ports: ["8000:8000"]
  model-manager:
    build: ./model_manager
    ports: ["8001:8000"]
  web:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [api, model-manager]
